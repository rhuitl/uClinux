===============================================================================
    LIBNET 1.0 (c) 1998 - 2001 Mike D. Schiffman <mike@infonexus.com>
                               http://www.packetfactory.net/libnet
===============================================================================


    1.1 TODO list

    - libnet_name_resolve returns -1 upon error which equates to
      255.255.255.255.  Paramterize this function (breaking compatibility).
      Thankz for Kyle Hargraves
      for pointing this out.
    - Completely redesign the interface.  Proposed is a `blob` interface
      incorporating a generic libnet blob datatype that is cast into a
      packet type by way of a macro.

    - Portable arp lookup, which necessitaes portable route table lookups.

    - Absorb libpcap into libnet?

EOF
