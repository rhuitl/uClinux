# Put "your_username * your_password" here, e.g.
#   johndoe * supersecret
#
# The extra asterisk is here because more advanced pppd configurations
# (other than dial-isp-only) can have other values instead, so don't
# leave it out
