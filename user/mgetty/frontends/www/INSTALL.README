To install the package  (version 0.9)

1. go into your webdirectory
2. create a directory where the files should be installed, eg:
   /home/www/mgetty+sendfax/ (where /home/www/ is the webservers
   root-directory)
3. unpack the webfax-tar-file in this directory (created in 2)
4. configure the create-File
5. call create
