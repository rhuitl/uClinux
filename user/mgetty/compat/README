In this directory (mgetty/compat) you'll find various programs that are
needed to make mgetty+sendfax work on not-so-compatible platforms.

mg.echo.c:  a replacement for the SystemV echo program, which can do 
	    escape characters, taken from GNU shellutils 1.92 (modified).

newslock.c: an "atomic test-and-set" link operation for files, to be
	    called from shell scripts. Taken from C-News (unmodified).
